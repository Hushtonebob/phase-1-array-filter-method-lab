const drivers = ['Bobby', 'Sammy', 'Sally', 'Annette', 'Sarah']


function findMatching(arry,string){
    let newDrivers = arry.filter(blam);
    function blam(string){
        return string = true; 
    }
        return newDrivers;
};